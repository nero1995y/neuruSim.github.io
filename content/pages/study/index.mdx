---
title: 'Study'
slug: '/study'
cover: './about-us-cover-image.jpg'
---

# study

---

## HTTP 메서드 활용

- 2022 04 04

### 클라이언트에서 서버로 데이터 전송

#### summary

- 쿼리 파라미터를 통한 데이터 전송
  - GET
  - 정렬 필터
- 메세지 바디를 통한 데이터 전송
  - POST, PUT, PTACH

### content

- 정적 데이터 조회
  - 이미지 , 정적 텍스트 문서
  - 조회는 GET 사용
  - 쿼리 파라미터 없이 리소스 경로로 단순하게 조회
- 동적 데이터 조회
  - 쿼리파미터를 기반으로 정렬
  - 검색, 게시판 목록 필터
  - 조회 조건을 줄여주는 필터, 조회 결과를 정렬하는 정렬 조건
  - 조회는 GET
  - 쿼리 파라미터 사용하여 데이터 전달
- HTML Form 데이터 전송
  - POST 전송
    - form태그
      - Content Type application/x-www-form-urlencoded
      - 쿼리 파라미터와 거의 유사하게 전송 바디
  - GET 전송으로 바꾸면 쿼리파라미터로 넣는다
  - multipart-form data
    - 바이너리

## HTTP API 데이터 전송

- 서버 간 통신
- 앱 클라이언트
- 웹 클라이언트
  - AJAX
- post, put, patch 메세지 바디를 통해 데이터 전송
- get 조회 쿼리 파라미터로 데이터 전달
- Content-Type : application/json
  - TEXT, XML, JSON 등등

## HTTP API 설계 예시

회원 목록 /members - GET

회원 등록 /members - POST

회원 조회 /members /{id} GET

회원 수정 /members/{id} PATCH, PUT, POST

회원 삭제 /members/{id} DELETE

수정할때는 PATCH를 하는것이 안전하다. PUT은 게시판을 수정할때 정도 사용하면 좋고 애매하면 POST

POST - 신규 자원 등록 특징

- 클라이언트는 등록될 리소스의 URI를 모른다
- 서버가 새로 등록된 리소스 URI를 생성해준다
- 컬렉션 ✨
  - 서버가 관리하는 리소스 디렉토리
  - 서버가 리소스의 URI를 생성하고관리

PUT 기반 등록

- 파일설계
- 파일 목록 /files → GET
- 파일 조회 /files/filename
- ...
- 파일 대량 등록 /files → POST

PUT- 신규 자원 등록 특징

- 클라이언트가 리소스 URI를 알고 있어야 한다.
  - 파일을 등록 할때 파일 이름을 알 고 있어야 한다
- 클라이언트가 직접 리소스의 URI를 생성해준다
  - POST는 그냥 리소스를 만들어서 내려주는것이고 PUT은 리소스 다 알고 등록함
- 스토어✨
  - 클라이언트가 관리하는 리소스 저장소
  - 클라이언트가 URI를 알고 관리

대부분 POST 기반으로 신규등록 PUT은 드물게 사용

---

### HTML FROM 사용

- GET, POST 지원
- 컨트롤 URI
  - GET, POST 제약이 있음
  - 애매한 경우 컨트롤 URI 사용

정리

- 문서
  - 단일 개념
- 컬렉션
  - 서버가 관리하는 리소스 디렉토리
- 스토어
  - 클라이언트가 관리하는 자원 저장소
- 컨트롤러
  - 해결하기 어려운 프로세스 추가 실행 동사를 직접 사용
